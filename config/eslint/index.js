/**
 * ESLint Configuration Index
 *
 * This file re-exports the ESLint configuration to maintain compatibility
 * with the original configuration paths.
 */

import eslintConfig from "./eslint.config.js";

export default eslintConfig;
